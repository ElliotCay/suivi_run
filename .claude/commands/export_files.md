Extract files content to use it on another LLM.
Fonctionnement et résultat attendu :
    Identification : Sélectionne tous les fichiers pertinents pour le contexte ou la discussion actuelle.
    Comportement de la commande : Une fois exécutée par l'utilisateur, cette commande devra :
        Itérer sur la liste des fichiers identifiés.
        Afficher le nom de chaque fichier.
        Afficher le contenu brut de chaque fichier entre des balises de code.
        Ajouter un séparateur visuel entre les fichiers.
    Format de réponse : Ne fournis que la commande dans un bloc de code, suivie d'une courte phrase listant les fichiers inclus.
